This directory is mounted by the docker-compose file into the container under
`/dashboard/.streamlit`.

The directory contains a config.toml file,
but it should also contain a secrets.toml file pointing to the database.